tags:
  - Contacts
summary: Yeni kişi oluştur
operationId: createContact
description: Kullanıcının rehberine yeni bir kişi ekler.
security:
  - bearerAuth: []
requestBody:
  required: true
  content:
    application/json:
      schema:
        $ref: "../../components/schemas/contact.yaml"
responses:
  "200":
    $ref: "../../components/responses/200.yaml"
